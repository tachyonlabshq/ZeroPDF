# ZeroPDF for {{PLATFORM_LABEL}}

This bundle is the install-ready ZeroPDF skill for {{PLATFORM_LABEL}}.

## Install

1. Download the matching zip for your platform.
2. Extract it.
3. Move the resulting `ZeroPDF` folder into your skills directory.
4. Point your agent runtime at the included `mcp.json` or the binary under `bin/`.

## Included files

- `README.md`
- `SKILL.md`
- `mcp.json`
- `bin/{{BINARY_NAME}}`

## Contract

- Contract version: `{{CONTRACT_VERSION}}`
- Tool schema version: `{{TOOL_SCHEMA_VERSION}}`
- Minimum compatible tool schema version: `{{MIN_COMPATIBLE_VERSION}}`

## Notes

- This zip contains only the binary for {{PLATFORM_LABEL}}.
- `mcp.json` is already pointed at `./bin/{{BINARY_NAME}}`.
- `scan-agent-comments` supports `@agent` note and highlight workflows.
